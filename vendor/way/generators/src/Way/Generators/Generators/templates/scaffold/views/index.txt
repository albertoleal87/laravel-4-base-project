<div class="row">
	{{ Form::btn_create( URL::route('{{models}}.create'), trans('forms.create') ) }}
</div>
<br>	
<div class="row table-responsive">
	<table class="table data-table">
		<thead>
			<tr>
				{{headings}}
				<th>Options</th>
			</tr>
		</thead>

		<tbody>
			@foreach (${{models}} as ${{model}})
				<tr>
					{{fields}}
					<td>
						{{ Form::open( ['route'=>['{{models}}.destroy', ${{model}}->id], 'method' => 'DELETE'] ) }}
							{{ Form::btn_show( URL::route('{{models}}.show', ${{model}}->id ) ) }}
							{{ Form::btn_edit( URL::route('{{models}}.edit', ${{model}}->id ) ) }}
							{{ Form::btn_delete( URL::route('{{models}}.destroy',${{model}}->id)  ) }}
						{{ Form::close() }}
					</td>
				</tr>
			@endforeach
		</tbody>
	</table>
</div>